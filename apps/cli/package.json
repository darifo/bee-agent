{
  "name": "@bee-agent/cli",
  "version": "1.1.0",
  "private": true,
  "type": "module",
  "bin": {
    "bee": "./dist/main.js"
  },
  "scripts": {
    "build": "tsup src/main.ts --format esm --clean",
    "typecheck": "tsc --noEmit",
    "test": "vitest run --passWithNoTests",
    "bee": "node --env-file-if-exists=.env dist/main.js"
  },
  "dependencies": {
    "@bee-agent/client": "workspace:*",
    "@bee-agent/thread": "workspace:*",
    "commander": "^14.0.2"
  }
}
