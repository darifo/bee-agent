---
'@bee-agent/web': major
---

Rewrite the web client as a single conversation view driven by the item stream: a thread's SSE stream is reduced into transcript entries (user/assistant/tool/approval) and suspended turns surface an inline approval prompt. The v0 task console components are removed.
