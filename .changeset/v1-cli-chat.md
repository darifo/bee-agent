---
'@bee-agent/cli': major
---

Rewrite the CLI as a conversation client. `bee chat` starts an interactive thread and keeps turning user messages into turns, resolving approval prompts inline; `bee thread create` creates a thread. The v0 task/approval/memory command groups are removed.
